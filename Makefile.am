SUBDIRS = src doc 
#include aminclude.am
EXTRA_DIST = Doxyfile oldMakefile bootstrap doc/manual.pdf
#DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
distdir = $(PACKAGE)-$(VERSION)
